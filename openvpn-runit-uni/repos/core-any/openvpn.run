#!/bin/sh
[ -r conf ] && source ./conf
CONF_FILE=${CONF_FILE:=/etc/openvpn/openvpn.conf}
[[ $CONF_FILE == \/* ]] || CONF_FILE=/etc/openvpn/client/$CONF_FILE
cd $(dirname $CONF_FILE)
exec openvpn --config $CONF_FILE
